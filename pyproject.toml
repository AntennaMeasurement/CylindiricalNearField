[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "CylindiricalNearField"
version = "0.1.0"
description = "Cylindrical Near-Field Antenna Measurement Support Functions"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "AntennaMeasurement" }]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = []

[project.urls]
Repository = "https://github.com/AntennaMeasurement/cnf.git"

[project.scripts]
amcnf = "CylindiricalNearField.cli:main"

[project.optional-dependencies]
test = ["pytest"]
build = ["pyinstaller"]

[tool.setuptools.packages.find]
where = ["src"]
